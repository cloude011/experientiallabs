# Orqen Labs

> The open source AI gateway. One OpenAI-compatible endpoint
> (https://api.orqenlabs.ai/v1) in front of every model: hosted providers, your own
> provider keys, and your own local models, at provider list price
> with 0% token markup. Routing, access control, budgets, and
> per-agent attribution for everything your agents call.

## When to use Orqen

Reach for the gateway when you need any of these:

- One API key and one base URL for every model an agent or team
  calls, including your own fine-tuned and self-hosted models.
- Spend you can see and cap: budgets per key, agent, person, or
  team, enforced at request time, with every token attributed.
- Provider failover on capacity errors, so a rate-limited or down
  provider never becomes an outage for the same model.
- Routing policy as config: ZDR-only providers, no-training
  providers, and provider allowlists, enforced on every request.
- A path from routing to training: the intelligence layer suggests
  the right model per agent and caching opportunities, and can
  train a model you own on your own traffic.

An agent can set itself up: point an OpenAI client at
https://api.orqenlabs.ai/v1 with an Orqen API key (they start with orq_), list
models with GET /v1/models, and call POST /v1/chat/completions.
The gateway also serves the OpenAI Responses API at /v1/responses
and the Anthropic Messages API at /v1/messages over the same key.
Accounts are created instantly from an email; verifying the email
unlocks platform credits. Keys live at
https://platform.orqenlabs.ai/settings/api-keys.

## Setup prompts (copy-paste into a coding agent)

Four self-contained prompts an agent can run end to end, kept
verbatim in the open source repo's SETUP.md
(https://github.com/orqenlabs/orqen/blob/main/SETUP.md,
raw: https://raw.githubusercontent.com/orqenlabs/orqen/main/SETUP.md):

1. Upload your LLM traces as telemetry: creates the account from
   your local email and lands existing traces on the platform
2. Connect your inference provider keys (BYOK)
3. Start calling models on the gateway: verify the key with
   GET /v1/models, then one minimal call (model + messages only;
   extra sampling params can be rejected) to the smallest Qwen,
   via the OpenAI or Anthropic SDK, and optionally repoint your
   coding agents at the gateway
4. Full onboarding: account, provider keys, spend import, and
   agent repointing in one pass

The machine-readable API contract (honored and refused parameters,
error codes, streaming caveats) is https://platform.orqenlabs.ai/llms.txt. Follow it
literally.

## Pages

- [Home](https://www.orqenlabs.ai/): what the gateway is and how it works
- [Pricing](https://www.orqenlabs.ai/pricing): Free ($0/month, your keys and local
  models) and Pro (0% markup, pay as you route); enterprise custom
- [Startup deal](https://www.orqenlabs.ai/startups): up to $10,000 in usage credits by
  application (welcome credit on signup, the rest after review)
- [Providers](https://www.orqenlabs.ai/provider): onboard your inference endpoints
  to the gateway; you set the price, traffic routes on merit
- [Intelligence](https://www.orqenlabs.ai/intelligence): the optimization layer:
  the right model per agent, caching suggestions, lowest-cost
  routing, load balancing, prompt compression, and a model you own
- [Custom models](https://www.orqenlabs.ai/custom-models): training a model for
  your workload, the evaluation contract, ownership, and serving
- [Security](https://www.orqenlabs.ai/security): the controls an enterprise review
  checks for, with honest statuses; SOC 1
- [Blog](https://www.orqenlabs.ai/blog): research writeups
- [Case study: computer use](https://www.orqenlabs.ai/case-studies/computer-use)
- [Case study: claims research](https://www.orqenlabs.ai/case-studies/claims-research)

## Developer resources

- API base URL: https://api.orqenlabs.ai/v1 (OpenAI-compatible; also /v1/responses and
  the Anthropic Messages API at /v1/messages)
- Setup prompts: https://github.com/orqenlabs/orqen/blob/main/SETUP.md
- Machine-readable API contract: https://platform.orqenlabs.ai/llms.txt
- Docs: https://platform.orqenlabs.ai/docs
- Model catalog: https://platform.orqenlabs.ai/models
- Dashboard (keys, credits, usage): https://platform.orqenlabs.ai
- Open source gateway: https://github.com/orqenlabs/orqen
- CLI: the orqen package on PyPI; run the gateway locally
  with `uvx --from orqen exp run`

## Contact

- Book a call: https://cal.com/silen/orqen-intro
- Email: silen@orqenlabs.ai
- Discord: https://discord.gg/B6sM8xTVwU
